<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- 为 Theme 设置背景图 -->
    <style name="AppTheme.StartTheme" parent="android:style/Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@mipmap/lunach_1334</item>
    </style>
    <!-- 分享对话框样式 -->
    <style name="EditDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowFrame">@null</item> ：Dialog的windowFrame框为无
        <item name="android:windowIsFloating">true</item>：是否浮现在activity之上
        <item name="android:windowIsTranslucent">false</item>:是否半透明
        <item name="android:windowNoTitle">true</item>:是否显示title
        <item name="android:windowBackground">@drawable/edit_dialog_bg</item>:设置dialog的背景
        <item name="android:backgroundDimEnabled">true</item>背景是否模糊显示

    </style>

    <style name="dialog_style">
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowBackground">@color/transparent</item>:设置dialog的背景
        <item name="android:windowAnimationStyle">@style/dialog_anim</item>
    </style>
    <style name="dialog_anim">
        <item name="android:windowEnterAnimation">@anim/dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_anim</item>
    </style>

    <!--头像设置选择框-->
    <style name="HeadSelectDialog">
        <item name="android:windowFrame">@null</item> ：Dialog的windowFrame框为无
        <item name="android:windowBackground">@color/transparent</item>:设置dialog的背景
        <item name="android:backgroundDimEnabled">true</item>背景是否模糊显示
    </style>

    <!-- 分享对话框样式 -->
    <style name="TransparentDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@color/transparent_half</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- white dialog -->
    <style name="WhiteDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@color/dialog_white_bg</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabMaxWidth">@dimen/space_v_500</item>
        <item name="tabIndicatorColor">@color/confirm_bg_color</item>
        <item name="tabIndicatorHeight">@dimen/space_v_4</item>
        <item name="tabPaddingStart">@dimen/space_v_10</item>
        <item name="tabPaddingEnd">@dimen/space_v_10</item>
        <item name="tabBackground">@color/white</item>
        <item name="tabTextAppearance">@style/MyCustomTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/confirm_bg_color</item>
    </style>
    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/textsize_28</item>
        <item name="android:textColor">@color/text_color_1</item>
        <item name="textAllCaps">true</item>
    </style>




   </resources>
